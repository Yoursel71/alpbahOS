# Claude devir belgesi 002 — UI-02 (kısayol çakışma/yardım) ve UI-03 (Atatürk varlık planı)

Görev ID / durum: UI-02 taslak tamamlandı (statik, test edilmedi). UI-03 plan+kaynak adayları tamamlandı; görsel indirilmedi, kullanıcı onayı bekliyor.

Çalışılan host ve branch/commit: Windows host, worktree `C:\alpbahOS-claude`, dal `claude/desktop-bootstrap`. Bu belgenin yazıldığı anda henüz commit atılmadı; commit hash'i bu turun commit'iyle birlikte güncellenecek.

Değişen dosyalar:
- `profiles/shortcuts/conflicts.md` (yeni)
- `profiles/shortcuts/help.md` (yeni)
- `branding/ataturk-theme/README.md` (yeni)
- `docs/WORKLOG.md` (UI-02/UI-03 satırları eklendi)
- `docs/handoffs/claude/002-ui02-ui03.md` (bu dosya)

Gerçekleştirilen davranış:
- UI-02: `profiles/shortcuts/shortcuts.json`'daki (UI-01'den) 16 kısayol tek tek incelendi; iç tutarlılık kontrolü (çakışma yok), bilinen KWin/Plasma varsayılanlarıyla olası çakışma tablosu ve **Hyper-V/Windows host'un Win tuşu kombinasyonlarını yakalama riski** (bu makinede geliştirme Hyper-V üzerinden yapıldığı için gerçek, somut bir kısıt) kayda geçirildi. Ayrıca kısayolların kullanıcıya nasıl keşfettirileceğine dair bir yardım/help planı yazıldı (yeni bileşen icat edilmedi, Plasma'nın yerleşik Kısayollar/sıfırlama mekanizması kullanılıyor).
- UI-03: Web araması ile (`WebSearch`/`WebFetch`) gerçek Atatürk portre kaynak adayları arandı: Wikimedia Commons'taki iki "public domain" işaretli aday (210×275 ve 732×987 px, ikisi de **modern duvar kâğıdı için çok düşük çözünürlük**), bir CC BY-SA 4.0 lisanslı aday (public domain değil, atıf gerektirir) ve üç resmi devlet arşivi (ATAM, TCCB, TTK — muhtemelen daha yüksek çözünürlüklü ama tek tek lisans kontrolü gerekiyor) bulundu ve `branding/ataturk-theme/README.md`'ye kaynak/lisans/çözünürlük notlarıyla kaydedildi. Kırpım (16:9/16:10/ultrawide), kilit ekranı kontrast ve logo koruma kuralları MASTER_PLAN §7.1 ve tasarım dokümanına dayanarak yazıldı.

Çalıştırılan doğrulama ve sonuç:
- UI-02: Yalnızca elle/statik tutarlılık kontrolü (JSON içindeki tuş kombinasyonları arasında birebir çakışma var mı). Gerçek KWin `kglobalaccel` kayıt denetimi **yapılmadı** (Plasma oturumu yok).
- UI-03: Web araması gerçekten yapıldı ve sonuçlar doğrulandı (WebFetch ile iki sayfanın lisans/çözünürlük bilgisi çekildi). **Hiçbir görsel dosyası indirilmedi veya repoya eklenmedi** — bu bilinçli bir sınır: dosya indirme bu oturumda kullanıcı onayı gerektiren bir eylem sınıfı.

Log / ekran görüntüsü / artifact: Yok.

Bilinen sorun ve açık karar:
1. UI-02: `Win` (tek başına) ve `Win+Tab` satırları en yüksek çakışma riskini taşıyor; gerçek Plasma sürümünde doğrulanmadan üzerine yazılmamalı.
2. UI-02: Hyper-V geliştirme döngüsünde `Win+L`/`Win+D`/`Win+Tab` gibi kombinasyonların çoğu host Windows tarafından yakalanabilir — gerçek test için çıplak donanım veya Win tuşunu host'a kaptırmayan bir erişim yöntemi (VNC/SPICE) gerekiyor.
3. UI-03: Kolayca bulunan public-domain adaylar çözünürlük olarak yetersiz; ya resmi arşivlerden (ATAM/TCCB/TTK) daha yüksek çözünürlüklü bir tarama bulunmalı ya da stilize/vektör bir versiyon üretilmeli. Hiçbiri bu turda üretilmedi.
4. UI-03: Boot ekranı bu planın kapsamı dışında (MASTER_PLAN §7.1: ayrıca karara bağlanmamış).

Entegrasyon için gereken:
- UI-02'nin gerçek testi Codex'in M07/M08'i tamamlamasını bekliyor.
- UI-03'te kullanıcının bir kaynak adayı onaylaması ve indirme izni vermesi gerekiyor; onaydan sonra kaynak/lisans bilgisi bu belgeye ve `branding/ataturk-theme/README.md`'ye işlenecek.

Sonraki eylem: Kullanıcı UI-03 için bir kaynak/yaklaşım seçerse (resmi arşiv taraması vs. stilize versiyon) o yönde ilerlenir; aksi halde sıradaki backlog kalemi Codex'in M-serisi ilerlemesine bağlı.
