# M02 `alp` automated test evidence — 24 September 2026

## Run

- Repository: `C:\alpbahOS`, branch `main`.
- Runtime: Windows Python 3.14.7, pytest 8.4.2.
- Command: `python -m pytest docs/handoffs/claude/alp-prototype/tests/ -q -rs`.
- Terminal: TTY-enabled PowerShell session (needed because this Windows test
  suite launches child `alp.py` processes with captured pipes).
- Result: **82 passed, 10 skipped** in 4.28 seconds.
- No network access is performed by the pytest suite; it uses temporary roots,
  local files, and mocked download functions.

## Skips

Seven tests require POSIX symlink privileges/behavior unavailable in this
Windows environment; one requires POSIX permission bits; one requires a FIFO;
one checks POSIX stale-lock detection. This is a count of test cases, so the
symlink-related skips account for seven of the ten.

## Scope and limits

The run verifies the local `alp.py` core logic and PackageKit wrapper
subprocesses. It does not prove a PackageKit service/Discover GUI integration,
the real `update` command against a repository, installation or upgrade on the
current `/mnt/lfs` rootfs, rollback after power loss, or ownership records for
the existing LFS base. A previous separate Gen2 test image passed the `htop`
install/list/run/remove flow; that was not repeated in the current audio test
image. M02 therefore remains partial and M04 base ownership remains open.

An initial non-TTY invocation ran 76 passing, 10 skipped, and 6 failing due
Windows `subprocess.Popen` `DuplicateHandle` errors (`WinError 6`). Running the
PackageKit tests individually with a TTY passed, and the complete suite then
passed at 82/10. The initial failure is retained as a runner/terminal-mode
limitation, not hidden as a package code result.
